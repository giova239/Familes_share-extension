<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    android:layout_height="wrap_content"
    android:layout_width="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="horizontal"
    android:layout_marginVertical="5dp"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <ImageView
        android:id="@+id/announcementIcon"
        android:adjustViewBounds="true"
        android:scaleType="centerCrop"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:src="@drawable/ic_image_missing"
        android:layout_margin="10dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"/>

    <TextView
        android:id="@+id/annoncementTitle"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:text="Error 404"
        android:textSize="25dp"
        android:layout_marginHorizontal="10dp"
        app:layout_constraintLeft_toRightOf="@+id/announcementIcon"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="@+id/announcementIcon"
        app:layout_constraintBottom_toBottomOf="@id/announcementIcon"/>

</androidx.constraintlayout.widget.ConstraintLayout>